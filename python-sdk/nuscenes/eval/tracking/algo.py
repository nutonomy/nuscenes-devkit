def accumulate(gt_boxes: EvalBoxes,
               pred_boxes: EvalBoxes,
               class_name: str,
               dist_fcn_name: str,
               dist_th: float,
               verbose: bool = False):
    pass  # TODO